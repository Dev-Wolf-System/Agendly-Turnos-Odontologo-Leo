# Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=avax_health
DB_PASSWORD=change_me
DB_DATABASE=Avax_Health_BDs

# JWT (legacy — solo para compatibilidad interna)
JWT_SECRET=change_me_to_random_64_chars
JWT_EXPIRATION=3600
JWT_REFRESH_EXPIRATION=604800

# Supabase Auth — el JWT_SECRET de tu instancia Supabase (dashboard → Settings → API)
SUPABASE_JWT_SECRET=change_me_to_supabase_jwt_secret
SUPABASE_URL=https://supabase.avaxhealth.com
SUPABASE_SERVICE_ROLE_KEY=change_me_to_supabase_service_role_key

# App
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# Agent API (n8n / Zoe)
AGENT_API_KEY=change_me_to_secure_api_key

# Mercado Pago (Billing)
MP_ACCESS_TOKEN=APP_USR-xxxx
MP_WEBHOOK_SECRET=change_me_to_mp_webhook_secret
BACKEND_URL=https://api.avaxhealth.com/api
